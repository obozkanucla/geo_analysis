# Core data packages
pandas==2.0.3
numpy==1.25.2
matplotlib==3.7.5
geopandas>=1.1.1   # allow modern version

# Web & dashboard
streamlit==1.40.1
folium==0.18.0
streamlit-folium==0.22.1

# Optional for nicer maps // geoprocessing
shapely>=2.0
pyproj>=3.5
fiona>=1.10
geopy>=2.4.1          # for postcode geocoding (Nominatim)
requests>=2.31.0      # used by geopy under the hood

# =============================
# ⚙️ System utils (safe defaults)
# =============================
python-dateutil>=2.8
tqdm>=4.65.0          # for progress bars if you later use them